# Plan — `sample-task-missing-sections`

## Objective

This fixture is intentionally missing required sections for lint failure tests.

## Scope

- Missing sections on purpose so `lint-task.sh` warns and exits non-zero.
